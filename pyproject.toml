[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "groundtruth"
version = "0.1.0"
description = "groundtruth — a tree-sitter code knowledge graph for token-efficient code retrieval and generation."
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [{ name = "Vinod Narayanswamy" }]
keywords = ["groundtruth", "tree-sitter", "knowledge-graph", "llm", "code-retrieval", "rag", "ast"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Software Development :: Libraries",
]
dependencies = [
    "tree-sitter>=0.23",
    "tree-sitter-python>=0.23",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[project.urls]
Homepage = "https://github.com/vinodnarayanswamy/groundtruth"
Issues = "https://github.com/vinodnarayanswamy/groundtruth/issues"

[project.scripts]
groundtruth-demo = "examples.demo:main"

[tool.hatch.build.targets.wheel]
packages = ["groundtruth"]
